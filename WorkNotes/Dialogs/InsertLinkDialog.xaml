<Window x:Class="WorkNotes.Dialogs.InsertLinkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insert Link"
        Height="240"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label -->
        <TextBlock Grid.Row="0" 
                   Text="Label (optional):"
                   Margin="0,0,0,4"/>
        <TextBox x:Name="LabelTextBox"
                 Grid.Row="1"
                 Padding="8,6"
                 Margin="0,0,0,12"/>

        <!-- URL -->
        <TextBlock Grid.Row="2" 
                   Text="URL:"
                   Margin="0,0,0,4"/>
        <TextBox x:Name="UrlTextBox"
                 Grid.Row="3"
                 Padding="8,6"
                 Margin="0,0,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="OK"
                    Click="OK_Click"
                    IsDefault="True"
                    Width="80"
                    Height="32"
                    Margin="0,0,8,0"
                    Background="{DynamicResource App.Accent}"
                    Foreground="White"
                    FontWeight="SemiBold"/>
            <Button Content="Cancel"
                    Click="Cancel_Click"
                    IsCancel="True"
                    Width="80"
                    Height="32"/>
        </StackPanel>
    </Grid>
</Window>
